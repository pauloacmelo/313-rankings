import React from 'react';
import ReactDOM from 'react-dom/client';
import CrossfitLeaderboardApp from './CrossfitLeaderboardApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <CrossfitLeaderboardApp />
  </React.StrictMode>
); 